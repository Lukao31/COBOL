SELECT DISTINCT FP.DESCFORMA
FROM FORMAPGTO FP
INNER JOIN COMPAGTO CP ON FP.CODFORMA = CP.CODFORMA
INNER JOIN PEDIDO PD ON CP.NUMPED = PD.NUMPED
INNER JOIN CLIENTE CL ON PD.CODCLI = CL.CODCLI
WHERE CL.NOMECLI = 'ANA MARIA'
