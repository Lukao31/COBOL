-- CREATE TABLE CONTA
--  (
--    IDCONTA    INTEGER NOT NULL
--   ,IDCLIENTES INTEGER NOT NULL
--   ,TIPOCONTA  CHAR(2) NOT NULL
--               CHECK(TIPOCONTA IN ('CC','CP'))
--   ,SALDO      DECIMAL(9,2) NOT NULL CHECK(SALDO >= 0)
--   ,STATUS     CHAR(1) NOT NULL CHECK(STATUS IN ('A','B'))
--  );

-- CREATE UNIQUE INDEX IDXCONTA
--  ON CONTA  (IDCONTA);

-- ALTER TABLE CONTA
--  ADD PRIMARY KEY (IDCONTA);

-- ALTER TABLE CONTA
--  ADD CONSTRAINT FK_CONTAS_CLIENTES
--  FOREIGN KEY (IDCLIENTES)
--  REFERENCES CLIENTES (IDCLIENTE);

--  DROP TABLE CONTA
    SELECT *
    FROM CONTA
