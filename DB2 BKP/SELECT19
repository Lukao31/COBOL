SELECT SUM(V.QTDEVENDA * P.PRECOPROD)
FROM TBVENDA V, TBPROD P
WHERE V.CODCLI = '0001'
  AND YEAR(DATAHORAVENDA) = YEAR(CURRENT DATE)
  AND MONTH(DATAHORAVENDA) = YEAR(CURRENT DATE)
                                                                        00010000
